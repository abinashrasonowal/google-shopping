{
  "openapi": "3.0.0",
  "info": {
    "title": "Google Shopping Scraper",
    "version": "1.0.0",
    "description": "Scrapes product listings from Google Shopping including price, rating, reviews, and store info"
  },
  "paths": {
    "/run-sync-get-dataset-items": {
      "post": {
        "operationId": "runScraper",
        "summary": "Run scraper and return products",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["query"],
                "properties": {
                  "query": {
                    "type": "string",
                    "description": "Search query e.g. wireless headphones"
                  },
                  "country": {
                    "type": "string",
                    "description": "2-letter country code e.g. in, us, gb",
                    "default": "in"
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "List of scraped products",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "position":     { "type": "integer" },
                      "title":        { "type": "string", "nullable": true },
                      "url":          { "type": "string", "format": "uri" },
                      "price":        { "type": "string", "nullable": true },
                      "second_hand_condition": { "type": "string", "nullable": true, "enum": ["pre-owned", "refurbished", "used", null] },
                      "rating":       { "type": "number", "nullable": true },
                      "review_count": { "type": "integer", "nullable": true },
                      "image":        { "type": "string", "format": "uri", "nullable": true },
                      "source":       { "type": "string", "nullable": true }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}